<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.b2b.mapper.ProductMapper">

	<insert id="create">
		INSERT INTO TBL_PRODUCT (PNO, PNAME, CATEGORY, CONTENT, STARTUP_ID)
			VALUES (SEQ_PRODUCT.nextval, #{pname}, #{category}, #{content}, #{startup_id})
	</insert>
	
	<select id="read" resultType="com.b2b.domain.ProductVO">
		SELECT PNO, PNAME, CATEGORY, CONTENT, STARTUP_ID, REGDATE
			FROM TBL_PRODUCT
			WHERE PNO = #{pno}
	</select>
	
	<update id="update">
		UPDATE TBL_PRODUCT SET PNAME = #{pname}, CATEGORY = #{category}, CONTENT = #{content}, STARTUP_ID = #{startup_id}
			WHERE PNO = #{pno}
	</update>
	
	<delete id="delete">
		DELETE FROM TBL_PRODUCT WHERE PNO = #{pno}
	</delete>

</mapper>