<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<beans:bean id="LoginInterceptor"
		class="com.b2b.interceptor.LoginInterceptor"></beans:bean>
		
	<beans:bean id="AuthInterceptor"
		class="com.b2b.interceptor.AuthInterceptor"></beans:bean>

	<interceptors>

		<interceptor>
			<mapping path="/startupUser/loginPost" />
			<mapping path="/buyerUser/loginPost" />
			<beans:ref bean="LoginInterceptor" />
		</interceptor>

		<interceptor>
			<mapping path="/web/startup/register" />
			<mapping path="/web/startup/modifyPage" />
			<mapping path="/web/startup/removePage" />
			<mapping path="/web/buyer/register" />
			<mapping path="/web/buyer/modifyPage" />
			<mapping path="/web/buyer/removePage" />
			<beans:ref bean="AuthInterceptor" />

		</interceptor>

	</interceptors>

	<!--파일 업로드 관련 Beans 설정 -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="10485760"></beans:property>
	</beans:bean>
	<!-- 서버의 파일 저장 경로 설정 -->
	<beans:bean id="uploadPath" class="java.lang.String">
		<beans:constructor-arg
			value="C:\\resources\\upload"></beans:constructor-arg>
	</beans:bean>

	<context:component-scan base-package="com.b2b.controller" />

</beans:beans>
